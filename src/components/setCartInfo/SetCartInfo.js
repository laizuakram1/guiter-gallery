import React from 'react';

const SetCartInfo = ({guiter}) => {
    // console.log(guiter);
    return (
        <div>
            <h4>{guiter.name}</h4>
        </div>
    );
};

export default SetCartInfo;